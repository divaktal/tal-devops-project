name: CI/CD Pipeline

permissions:
  contents: write

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  lint:
    name: Lint and Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Validate Files
        run: |
          [ -f public/index.html ] && echo "✅ HTML exists" || exit 1
          [ -f public/styles.css ] && echo "✅ CSS exists" || echo "⚠ No CSS"
          [ -f public/script.js ] && echo "✅ JS exists" || echo "⚠ No JS"

  build:
    name: Build and Test
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Create artifact
        run: |
          mkdir -p dist
          cp -r public/* dist/ 2>/dev/null || true
          ls -la dist/
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-app
          path: dist/

  docker:
    name: Build, Push to ECR and Update Helm
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      
      - name: Build, tag, and push image to Amazon ECR
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          REPOSITORY: lily-designer-studio
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .
          docker tag $REGISTRY/$REPOSITORY:$IMAGE_TAG $REGISTRY/$REPOSITORY:latest
          
          docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG
          docker push $REGISTRY/$REPOSITORY:latest
          
          docker tag $REGISTRY/$REPOSITORY:$IMAGE_TAG my-app:latest

          echo "image_uri=$REGISTRY/$REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
      
      - name: Start PostgreSQL
        run: |
          docker network create app-net || true
          docker run -d --name test-db --network app-net \
            -e POSTGRES_DB=test_db \
            -e POSTGRES_USER=test_user \
            -e POSTGRES_PASSWORD=test123 \
            -p 5432:5432 \
            postgres:13
          
          echo "Waiting for PostgreSQL..."
          sleep 10

      - name: Test Docker container
        run: |
          docker run -d --name test-app --network app-net \
            -e DB_HOST=test-db \
            -e DB_PORT=5432 \
            -e DB_NAME=test_db \
            -e DB_USER=test_user \
            -e DB_PASSWORD=test123 \
            -e PORT=3000 \
            -p 3000:3000 \
            my-app:latest
          
          echo "Waiting for app to start..."
          timeout 60 bash -c 'until curl -f http://localhost:3000/api/health > /dev/null 2>&1; do sleep 2; done' || echo "Health check timeout"
          
          curl -f http://localhost:3000/ && echo "✅ Root endpoint OK" || echo "❌ Root endpoint failed"
          curl -f http://localhost:3000/metrics && echo "✅ Metrics endpoint OK" || echo "❌ Metrics endpoint failed"
          
          if curl -f http://localhost:3000/api/health; then
            echo "✅ All tests passed"
          else
            echo "❌ Health check failed"
            docker logs test-app
            exit 1
          fi

      - name: Update Helm chart with new image tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_URL: https://github.com/divaktal/tal-devops-project.git
        run: |
          echo "Updating Helm chart with new image tag: ${{ github.sha }}"
          
          git clone $REPO_URL helm-repo
          cd helm-repo
          
          sed -i "s|tag:.*|tag: \"${{ github.sha }}\"|" helm/values.yaml
          
          echo "Updated values.yaml:"
          grep "tag:" helm/values.yaml
          
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          
          git add helm/values.yaml
          git commit -m "CI: Update image tag to ${{ github.sha }}"
          git push https://x-access-token:$GITHUB_TOKEN@github.com/divaktal/tal-devops-project.git HEAD:master
          
          echo "✅ Helm chart updated successfully"

      - name: Trigger ArgoCD Sync
        if: success()
        env:
          ARGOCD_SERVER: ${{ secrets.ARGOCD_SERVER }}
          ARGOCD_TOKEN: ${{ secrets.ARGOCD_TOKEN }}
        run: |
          if [ -n "$ARGOCD_TOKEN" ] && [ -n "$ARGOCD_SERVER" ]; then
            echo "🚀 Triggering ArgoCD sync"
            
            curl -s -X POST \
              "$ARGOCD_SERVER/api/v1/applications/lily-designer-studio/sync" \
              -H "Authorization: Bearer $ARGOCD_TOKEN" \
              -H "Content-Type: application/json" \
              -d '{"prune": true, "dryRun": false}'
            
            echo "📊 Checking ArgoCD sync status..."
            sleep 5
            
            curl -s \
              "$ARGOCD_SERVER/api/v1/applications/lily-designer-studio" \
              -H "Authorization: Bearer $ARGOCD_TOKEN" | jq -r '.status.sync.status, .status.health.status' || true
            
          else
            echo "⚠️ ArgoCD sync not configured"
          fi

      - name: Cleanup
        if: always()
        run: |
          docker stop test-app || true
          docker stop test-db || true
          docker rm test-app || true
          docker rm test-db || true
          docker network rm app-net || true
